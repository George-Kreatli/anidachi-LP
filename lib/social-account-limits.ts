/**
 * Global max connected accounts per platform.
 * OAuth callbacks reject new accounts beyond this; existing accounts over the limit are grandfathered.
 */
export const MAX_INSTAGRAM_ACCOUNTS = 5;
export const MAX_TIKTOK_ACCOUNTS = 5;
